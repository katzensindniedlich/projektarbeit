import { defineConfig } from 'astro/config'


export default defineConfig(
    {
        site: 'https://projektarbeit.pages.dev',
        build: {
            assets: 'assets'
        }
    }
)